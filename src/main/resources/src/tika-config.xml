<?xml version="1.0" encoding="UTF-8"?>
<properties>
    <!-- This prevents tika from outputting optional dependency warnings on System.err -->
    <service-loader initializableProblemHandler="ignore"/>
</properties>
